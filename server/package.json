{
  "name": "server",
  "version": "1.0.0",
  "description": "Node.js server",
  "main": "dist/index.js",
  "outDir": "dist",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prebuild": "rm -rf dist",
    "postbuild": "cp -r ../shared/dist/ dist/shared/ && cp -r ../ui/dist/ dist/public/",
    "dev": "npx nodemon",
    "start": "cross-env NODE_PATH=./dist node dist/index.js",
    "test": "vitest"
  },
  "author": "omdinh",
  "license": "ISC",
  "dependencies": {
    "@notebook/shared": "1.0.0",
    "body-parser": "^2.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.19",
    "cross-env": "^10.1.0",
    "nodemon": "^3.0.1",
    "tsconfig-paths": "^4.2.0",
    "ts-node": "^10.9.1"
  }
}
