import NativeButton from "components/shared/NativeButton";

export const Navbar = () => {
  return (
    <nav>
        <NativeButton>Click me</NativeButton>
    </nav>
    );
};

export default Navbar;
